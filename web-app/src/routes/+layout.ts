// Fully client-rendered: geolocation and the Maps JS SDK are browser-only,
// and there's no meaningful content to server-render for this app anyway.
export const ssr = false;
export const prerender = true;
